# Global plugins and directories
sieve_plugins = sieve_imapsieve sieve_extprograms
sieve_pipe_bin_dir = /data/sieve

# Matches example: sieve_extensions block
sieve_extensions {
  mboxmetadata = yes
  imapflags = yes
  notify = yes
  spamtest = yes
  spamtestplus = yes
  virustest = yes
  editheader = yes
  servermetadata = yes
  environment = yes
}

# Matches example: sieve_global_extensions block
sieve_global_extensions {
  vnd.dovecot.pipe = yes
  vnd.dovecot.environment = yes
  vnd.dovecot.debug = yes
}

# Matches example: sieve_script blocks
sieve_script personal {
  driver = file
  path = /data/sieve/%{user}.sieve
}

sieve_script default {
  type = default
  driver = file
  path = /data/sieve/default.sieve
}

sieve_script spam_filter {
  type = before
  driver = file
  path = /data/sieve/moveToPotentialSpam.sieve
}

# Configuration for spamtest (separate block for 2.4)
sieve_spamtest {
  status_type = text
  status_header = X-Spam-Flag
  text_value1 = NO
  text_value10 = YES
}
