# This file will be regenerated at startup by init-dovecot/run
# Doveadm password is configured via DOVEADM_PASSWORD environment variable
# See /etc/dovecot/conf.d/91-doveadm.conf for actual configuration
