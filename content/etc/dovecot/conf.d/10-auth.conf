# Authentication mechanisms: plain and login require TLS
# plain = RFC 4616 PLAIN mechanism
# login = Outlook-style LOGIN mechanism (legacy)
auth_mechanisms = plain login

!include auth-sql.conf.ext

# Rate limiting and security
service auth {
  # Prevent brute force attacks
  limit = 512
  limit_except_key = %{real_lip}
}